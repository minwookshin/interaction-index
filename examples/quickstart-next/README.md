# Teum + Next.js quickstart

This App Router starter is exercised by `npm run test:adoption:next`. The verifier copies it to a clean directory, connects the pinned Teum registry, installs Button, and requires strict TypeScript and a Next.js production build.

The interactive proof lives behind one explicit client boundary. The page and root layout remain Server Components, and the global product stylesheet owns one semantic radius override.
