name: Public beta health

on:
  schedule:
    - cron: "17 13 * * *"
  workflow_dispatch:

concurrency:
  group: public-beta-health
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.14.0
          cache: npm
      - run: npm ci
      - run: npm run check:freeze
      - run: npm run check:beta
      - run: npm run check:security
      - name: Verify live public beta
        run: npm run verify:public-beta -- --write-evidence
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: teum-public-beta-health-${{ github.run_number }}
          path: |
            release/public-beta-live.json
            release/beta-readiness.json
            release/public-surface.freeze.json
            release/registry-history.json
          if-no-files-found: error
          retention-days: 35
