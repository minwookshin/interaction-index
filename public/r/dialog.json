{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "dialog",
  "title": "Dialog",
  "dependencies": [
    "@base-ui/react",
    "@phosphor-icons/react"
  ],
  "registryDependencies": [
    "@index/icon-button"
  ],
  "files": [
    {
      "path": "registry/components/ui/dialog.tsx",
      "content": "import \"../../styles/index-base.css\";\nimport \"../../styles/components/dialog.css\";\nimport { Dialog as DialogPrimitive } from \"@base-ui/react/dialog\";\nimport { X } from \"@phosphor-icons/react\";\nimport { useRef, type HTMLAttributes, type MutableRefObject, type Ref } from \"react\";\nimport { cn } from \"../../lib/cn\";\nimport { IconButton } from \"./icon-button\";\n\nexport const Dialog = DialogPrimitive.Root;\nexport const DialogTrigger = DialogPrimitive.Trigger;\nexport const DialogClose = DialogPrimitive.Close;\nexport const DialogTitle = DialogPrimitive.Title;\nexport const DialogDescription = DialogPrimitive.Description;\n\nfunction assignRef<T>(ref: Ref<T> | undefined, value: T | null) {\n  if (typeof ref === \"function\") ref(value);\n  else if (ref) (ref as MutableRefObject<T | null>).current = value;\n}\n\nexport function DialogContent({ className, children, showClose = true, initialFocus, ref, ...props }: DialogPrimitive.Popup.Props & { showClose?: boolean }) {\n  const popupRef = useRef<HTMLDivElement | null>(null);\n  const setPopupRef = (node: HTMLDivElement | null) => {\n    popupRef.current = node;\n    assignRef(ref, node);\n  };\n\n  return (\n    <DialogPrimitive.Portal>\n      <DialogPrimitive.Backdrop className=\"ix-dialog__backdrop\" />\n      <DialogPrimitive.Popup\n        ref={setPopupRef}\n        className={cn(\"ix-dialog\", className)}\n        initialFocus={initialFocus ?? ((openType) => openType === \"keyboard\" ? true : popupRef.current)}\n        {...props}\n        data-layer=\"modal\"\n      >\n        {children}\n        {showClose && (\n          <DialogPrimitive.Close render={<IconButton className=\"ix-dialog__close\" variant=\"ghost\" size=\"small\" aria-label=\"Close dialog\" />}>\n            <X />\n          </DialogPrimitive.Close>\n        )}\n      </DialogPrimitive.Popup>\n    </DialogPrimitive.Portal>\n  );\n}\n\nexport function DialogHeader({ className, ...props }: HTMLAttributes<HTMLDivElement>) {\n  return <div className={cn(\"ix-dialog__header\", className)} {...props} />;\n}\n\nexport function DialogFooter({ className, ...props }: HTMLAttributes<HTMLDivElement>) {\n  return <div className={cn(\"ix-dialog__footer\", className)} {...props} />;\n}\n",
      "type": "registry:ui",
      "target": "components/ui/dialog.tsx"
    },
    {
      "path": "registry/styles/components/dialog.css",
      "content": "/* Generated from src/styles.css. Do not edit directly. */\n\n@layer index.tokens, index.base, index.components;\n\n@layer index.components {\n.ix-dialog__backdrop { position: fixed; inset: 0; z-index: var(--ix-layer-modal-backdrop); background: var(--ix-bg-scrim); backdrop-filter: blur(1.5px); transition: opacity var(--ix-enter-duration) var(--ix-ease-out); }\n\n.ix-dialog__backdrop[data-starting-style],\n.ix-dialog__backdrop[data-ending-style] { opacity: 0; }\n\n.ix-dialog__backdrop[data-ending-style] { transition-duration: var(--ix-exit-duration); }\n\n.ix-dialog { position: fixed; z-index: var(--ix-layer-modal); top: 50%; left: 50%; width: min(390px, calc(100vw - 32px)); padding: 16px; border-radius: var(--ix-radius-overlay); background: var(--ix-bg-modal); box-shadow: var(--ix-shadow-modal); outline: 0; transform: translate(-50%, -50%); transition: opacity var(--ix-enter-duration) var(--ix-ease-out), transform var(--ix-enter-duration) var(--ix-ease-out); }\n\n.ix-dialog[data-starting-style] { opacity: 0; transform: translate(-50%, calc(-50% + 6px)) scale(0.985); }\n\n.ix-dialog[data-ending-style] { opacity: 0; transform: translate(-50%, calc(-50% + 3px)) scale(0.99); transition-duration: var(--ix-exit-duration); }\n\n.ix-dialog__close { position: absolute; top: 8px; right: 8px; }\n\n.ix-dialog__header { display: grid; gap: 7px; padding-right: 28px; }\n\n.ix-dialog__header [data-slot=\"dialog-title\"],\n.ix-dialog__header h2 { margin: 0; font-size: 16px; font-weight: 600; letter-spacing: -0.015em; }\n\n.ix-dialog__header [data-slot=\"dialog-description\"],\n.ix-dialog__header p { margin: 0; color: var(--ix-ink-secondary); font-size: 13px; line-height: 1.55; }\n\n.ix-dialog__footer { display: flex; justify-content: flex-end; gap: 6px; margin-top: 20px; padding-top: 0; border-top: 0; }\n\n.ix-dialog { background: var(--ix-bg-modal); }\n\n@media (prefers-reduced-motion: reduce) {\n.ix-dialog,\n.ix-dialog__backdrop {\n    transition-property: opacity;\n    transition-duration: var(--ix-exit-duration);\n  }\n\n.ix-dialog[data-starting-style],\n.ix-dialog[data-ending-style] { transform: translate(-50%, -50%); }\n}\n}\n",
      "type": "registry:style",
      "target": "styles/components/dialog.css"
    }
  ],
  "type": "registry:ui"
}