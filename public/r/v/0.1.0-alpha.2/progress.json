{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "progress",
  "title": "Progress",
  "description": "Determinate and indeterminate task progress with accessible status.",
  "dependencies": [
    "@base-ui/react@^1.7.0"
  ],
  "registryDependencies": [
    "@index/interaction-index-base"
  ],
  "files": [
    {
      "path": "registry/components/ui/progress.tsx",
      "content": "import \"../../styles/index-base.css\";\nimport \"../../styles/components/progress.css\";\nimport { Progress as ProgressPrimitive } from \"@base-ui/react/progress\";\nimport type { CSSProperties } from \"react\";\nimport { cn } from \"../../lib/cn\";\n\nexport type ProgressProps = Omit<ProgressPrimitive.Root.Props, \"children\"> & {\n  label?: string;\n  \"aria-label\"?: string;\n  showValue?: boolean;\n  size?: \"small\" | \"medium\";\n  className?: string;\n};\n\nexport function Progress({ label, \"aria-label\": ariaLabel, showValue = true, size = \"medium\", className, value, min = 0, max = 100, style, ...props }: ProgressProps) {\n  const range = max - min;\n  const scale = typeof value === \"number\" && range > 0 ? Math.min(1, Math.max(0, (value - min) / range)) : 0.42;\n  const progressStyle = { ...style, \"--ix-progress-scale\": scale } as CSSProperties;\n  return (\n    <ProgressPrimitive.Root className={cn(\"ix-progress\", `ix-progress--${size}`, className)} value={value} min={min} max={max} aria-label={label ? undefined : ariaLabel ?? \"Progress\"} style={progressStyle} {...props}>\n      {(label || showValue) && <div className=\"ix-progress__meta\">{label ? <ProgressPrimitive.Label>{label}</ProgressPrimitive.Label> : <span />}{showValue && <ProgressPrimitive.Value />}</div>}\n      <ProgressPrimitive.Track className=\"ix-progress__track\">\n        <ProgressPrimitive.Indicator className=\"ix-progress__indicator\" />\n      </ProgressPrimitive.Track>\n    </ProgressPrimitive.Root>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/ui/progress.tsx"
    },
    {
      "path": "registry/styles/components/progress.css",
      "content": "/* Generated from src/styles.css. Do not edit directly. */\n\n@layer index.tokens, index.base, index.components;\n\n@layer index.components {\n.ix-progress { display: grid; width: min(320px, 100%); gap: 7px; }\n\n.ix-progress__meta { display: flex; align-items: center; justify-content: space-between; gap: 12px; color: var(--ix-ink-secondary); font-size: var(--ix-type-label); }\n\n.ix-progress__meta > span:last-child { color: var(--ix-ink-tertiary); font-variant-numeric: tabular-nums; }\n\n.ix-progress__track { position: relative; overflow: hidden; width: 100%; border-radius: 999px; background: var(--ix-gray-200); }\n\n.ix-progress__indicator { width: 100%; height: 100%; border-radius: inherit; background: var(--ix-gray-900); transform: scaleX(var(--ix-progress-scale)); transform-origin: left center; transition: transform 220ms var(--ix-ease-out, ease-out); }\n\n.ix-progress[data-indeterminate] .ix-progress__indicator { animation: ix-progress-indeterminate 1.15s var(--ix-ease-in-out, ease-in-out) infinite; }\n\n@media (prefers-reduced-motion: reduce) {\n.ix-progress[data-indeterminate] .ix-progress__indicator { animation-duration: 2.4s; }\n\n.ix-progress__indicator { transition-duration: 0.01ms; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n.ix-progress[data-indeterminate] .ix-progress__indicator { animation-duration: 2.4s; }\n}\n\n@keyframes ix-progress-indeterminate { from { transform: translateX(-72%) scaleX(.42); } to { transform: translateX(116%) scaleX(.42); } }\n}\n",
      "type": "registry:style",
      "target": "styles/components/progress.css"
    }
  ],
  "type": "registry:ui"
}