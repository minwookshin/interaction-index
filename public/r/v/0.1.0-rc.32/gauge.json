{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "gauge",
  "title": "Gauge",
  "description": "One bounded measure with explicit minimum, maximum, current value, and optional target marker.",
  "registryDependencies": [
    "@teum-pinned/teum-base",
    "@teum-pinned/analytics",
    "@teum-pinned/analytics-frame"
  ],
  "files": [
    {
      "path": "registry/components/ui/gauge.tsx",
      "content": "\"use client\";\n\nimport \"../../styles/teum-base.css\";\nimport \"../../styles/components/gauge.css\";\nimport { type CSSProperties, type ReactNode } from \"react\";\nimport { formatAnalyticsValue } from \"../../lib/analytics\";\nimport { cn } from \"../../lib/cn\";\nimport { AnalyticsFrame } from \"./analytics-frame\";\n\nexport type GaugeMarker = {\n  value: number;\n  label: string;\n};\n\nexport type GaugeProps = {\n  title: string;\n  description?: string;\n  value: number;\n  min?: number;\n  max?: number;\n  label?: string;\n  marker?: GaugeMarker;\n  className?: string;\n  height?: number;\n  valueFormatter?: (value: number) => string;\n  tone?: \"neutral\" | \"danger\";\n  loading?: boolean;\n  error?: ReactNode;\n  showDataByDefault?: boolean;\n};\n\nconst gaugeBox = { width: 640, height: 240, centerX: 320, centerY: 145, radius: 90, start: 150, end: 390 } as const;\n\nfunction clamp(value: number, min: number, max: number) {\n  return Math.min(max, Math.max(min, value));\n}\n\nfunction polar(angle: number, radius: number = gaugeBox.radius) {\n  const radians = (angle - 90) * Math.PI / 180;\n  return { x: gaugeBox.centerX + radius * Math.cos(radians), y: gaugeBox.centerY + radius * Math.sin(radians) };\n}\n\nfunction arcPath(startAngle: number, endAngle: number, radius: number = gaugeBox.radius) {\n  const start = polar(endAngle, radius);\n  const end = polar(startAngle, radius);\n  const largeArc = endAngle - startAngle <= 180 ? 0 : 1;\n  return `M ${start.x.toFixed(2)} ${start.y.toFixed(2)} A ${radius} ${radius} 0 ${largeArc} 0 ${end.x.toFixed(2)} ${end.y.toFixed(2)}`;\n}\n\nexport function Gauge({\n  title,\n  description,\n  value,\n  min = 0,\n  max = 100,\n  label = \"Current\",\n  marker,\n  className,\n  height = 240,\n  valueFormatter = formatAnalyticsValue,\n  tone = \"neutral\",\n  loading = false,\n  error,\n  showDataByDefault = false,\n}: GaugeProps) {\n  const validMinimum = Number.isFinite(min) ? min : 0;\n  const validMaximum = Number.isFinite(max) && max > validMinimum ? max : validMinimum + 1;\n  const validValue = Number.isFinite(value) ? clamp(value, validMinimum, validMaximum) : validMinimum;\n  const ratio = (validValue - validMinimum) / (validMaximum - validMinimum);\n  const endAngle = gaugeBox.start + (gaugeBox.end - gaugeBox.start) * ratio;\n  const markerRatio = marker ? (clamp(marker.value, validMinimum, validMaximum) - validMinimum) / (validMaximum - validMinimum) : null;\n  const markerAngle = markerRatio === null ? null : gaugeBox.start + (gaugeBox.end - gaugeBox.start) * markerRatio;\n  const markerInner = markerAngle === null ? null : polar(markerAngle, gaugeBox.radius - 11);\n  const markerOuter = markerAngle === null ? null : polar(markerAngle, gaugeBox.radius + 11);\n  const style = { \"--teum-gauge-progress\": `${ratio}` } as CSSProperties;\n\n  const plot = (\n    <div\n      className=\"teum-gauge__plot\"\n      data-tone={tone}\n      role=\"meter\"\n      aria-label={label}\n      aria-valuemin={validMinimum}\n      aria-valuemax={validMaximum}\n      aria-valuenow={validValue}\n      aria-valuetext={`${valueFormatter(validValue)} of ${valueFormatter(validMaximum)}`}\n      style={style}\n    >\n      <svg viewBox={`0 0 ${gaugeBox.width} ${gaugeBox.height}`} preserveAspectRatio=\"xMidYMid meet\" aria-hidden=\"true\" focusable=\"false\">\n        <path className=\"teum-gauge__track\" d={arcPath(gaugeBox.start, gaugeBox.end)} />\n        {ratio > 0 && <path className=\"teum-gauge__value\" d={arcPath(gaugeBox.start, endAngle)} />}\n        {markerInner && markerOuter && <line className=\"teum-gauge__marker\" x1={markerInner.x} y1={markerInner.y} x2={markerOuter.x} y2={markerOuter.y} />}\n      </svg>\n      <div className=\"teum-gauge__value-label\"><strong>{valueFormatter(validValue)}</strong><span>{label}</span></div>\n      <span className=\"teum-gauge__minimum\">{valueFormatter(validMinimum)}</span>\n      <span className=\"teum-gauge__maximum\">{valueFormatter(validMaximum)}</span>\n      {marker && <span className=\"teum-gauge__marker-label\">{marker.label} · {valueFormatter(marker.value)}</span>}\n    </div>\n  );\n  const table = (\n    <table>\n      <caption>{title} data</caption>\n      <thead><tr><th scope=\"col\">Measure</th><th scope=\"col\">Value</th></tr></thead>\n      <tbody>\n        <tr><th scope=\"row\">{label}</th><td>{valueFormatter(validValue)}</td></tr>\n        <tr><th scope=\"row\">Minimum</th><td>{valueFormatter(validMinimum)}</td></tr>\n        <tr><th scope=\"row\">Maximum</th><td>{valueFormatter(validMaximum)}</td></tr>\n        {marker && <tr><th scope=\"row\">{marker.label}</th><td>{valueFormatter(marker.value)}</td></tr>}\n      </tbody>\n    </table>\n  );\n  const markerSummary = marker ? ` ${marker.label} is ${valueFormatter(marker.value)}.` : \"\";\n  return <AnalyticsFrame className={cn(\"teum-gauge\", className)} title={title} description={description} height={height} summary={`${label} is ${valueFormatter(validValue)} on a range from ${valueFormatter(validMinimum)} to ${valueFormatter(validMaximum)}.${markerSummary}`} plotLabel={`${title} gauge`} plot={plot} table={table} loading={loading} error={error} showDataByDefault={showDataByDefault} />;\n}\n",
      "type": "registry:component",
      "target": "components/ui/gauge.tsx"
    },
    {
      "path": "registry/styles/components/gauge.css",
      "content": "/* Generated from src/foundation.css and src/styles.css. Do not edit directly. */\n\n@layer teum.tokens, teum.base, teum.components;\n\n@layer teum.components {\n.teum-gauge__plot { position: relative; width: 100%; height: 100%; color: var(--teum-fg-default); }\n\n.teum-gauge__plot svg { display: block; width: 100%; height: 100%; overflow: visible; }\n\n.teum-gauge__track,\n.teum-gauge__value { fill: none; stroke-linecap: round; stroke-width: 14; vector-effect: non-scaling-stroke; }\n\n.teum-gauge__track { stroke: color-mix(in oklch, var(--teum-fg-default) 8%, transparent); }\n\n.teum-gauge__value { stroke: var(--teum-fg-default); }\n\n.teum-gauge__plot[data-tone=\"danger\"] .teum-gauge__value { stroke: var(--teum-fg-danger); }\n\n.teum-gauge__marker { stroke: var(--teum-fg-muted); stroke-width: 1.5; vector-effect: non-scaling-stroke; }\n\n.teum-gauge__value-label { position: absolute; top: 45%; left: 50%; display: grid; justify-items: center; gap: 2px; transform: translate(-50%, -50%); }\n\n.teum-gauge__value-label strong { color: var(--teum-fg-default); font-size: 25px; font-variant-numeric: tabular-nums; font-weight: 620; letter-spacing: -.03em; }\n\n.teum-gauge__value-label span,\n.teum-gauge__minimum,\n.teum-gauge__maximum,\n.teum-gauge__marker-label { color: var(--teum-fg-subtle); font-size: var(--teum-type-metadata); font-variant-numeric: tabular-nums; }\n\n.teum-gauge__minimum,\n.teum-gauge__maximum { position: absolute; top: 75%; }\n\n.teum-gauge__minimum { left: calc(50% - 103px); }\n\n.teum-gauge__maximum { right: calc(50% - 103px); }\n\n.teum-gauge__marker-label { position: absolute; bottom: 13px; left: 50%; white-space: nowrap; transform: translateX(-50%); }\n}\n",
      "type": "registry:style",
      "target": "styles/components/gauge.css"
    }
  ],
  "meta": {
    "teum": {
      "contract": "/agent/teum-agent.json",
      "registryItem": "@teum/gauge"
    }
  },
  "type": "registry:component"
}
