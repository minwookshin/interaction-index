{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "chart",
  "title": "Chart",
  "description": "An interactive ordered-series chart with synchronized pointer and keyboard inspection plus a semantic data table.",
  "registryDependencies": [
    "@teum-pinned/teum-base",
    "@teum-pinned/analytics"
  ],
  "files": [
    {
      "path": "registry/components/ui/chart.tsx",
      "content": "\"use client\";\n\nimport \"../../styles/teum-base.css\";\nimport \"../../styles/components/chart.css\";\nimport {\n  useId,\n  useMemo,\n  useState,\n  type CSSProperties,\n  type KeyboardEvent,\n  type PointerEvent,\n  type ReactNode,\n} from \"react\";\nimport {\n  clampAnalyticsIndex,\n  createAnalyticsAreaPath,\n  createAnalyticsPath,\n  createAnalyticsTicks,\n  describeAnalyticsDatum,\n  formatAnalyticsValue,\n  getAnalyticsBandPosition,\n  getAnalyticsDomain,\n  getAnalyticsPointPosition,\n  getStackedAnalyticsDomain,\n  summarizeAnalyticsSeries,\n  type AnalyticsDatum,\n  type AnalyticsSeries,\n} from \"../../lib/analytics\";\nimport { cn } from \"../../lib/cn\";\n\nexport type ChartAnnotation = {\n  id: string;\n  index: number;\n  label: string;\n  tone?: \"neutral\" | \"danger\";\n};\n\nexport type ChartType = \"line\" | \"area\" | \"bar\" | \"stacked-bar\";\n\nexport type ChartProps = {\n  title: string;\n  description?: string;\n  data: readonly AnalyticsDatum[];\n  series: readonly AnalyticsSeries[];\n  className?: string;\n  height?: number;\n  includeZero?: boolean;\n  domain?: readonly [number, number];\n  type?: ChartType;\n  /** @deprecated Use type=\"area\". */\n  area?: boolean;\n  annotations?: readonly ChartAnnotation[];\n  valueFormatter?: (value: number, series: AnalyticsSeries) => string;\n  activeIndex?: number | null;\n  defaultActiveIndex?: number | null;\n  onActiveIndexChange?: (index: number | null) => void;\n  visibleSeries?: readonly string[];\n  defaultVisibleSeries?: readonly string[];\n  onVisibleSeriesChange?: (ids: readonly string[]) => void;\n  onDatumActivate?: (datum: AnalyticsDatum, index: number) => void;\n  loading?: boolean;\n  empty?: ReactNode;\n  error?: ReactNode;\n  showLegend?: boolean;\n  showDataByDefault?: boolean;\n  showGrid?: boolean;\n};\n\nconst chartBox = { width: 640, height: 264, left: 58, right: 16, top: 18, bottom: 34 } as const;\n\nfunction uniqueTickIndexes(length: number) {\n  if (length <= 1) return [0];\n  return [...new Set([0, Math.round((length - 1) * 0.25), Math.round((length - 1) * 0.5), Math.round((length - 1) * 0.75), length - 1])];\n}\n\nfunction useControllableValue<T>({ value, defaultValue, onChange }: { value: T | undefined; defaultValue: T; onChange?: (next: T) => void }) {\n  const [internal, setInternal] = useState(defaultValue);\n  const controlled = value !== undefined;\n  const current = controlled ? value : internal;\n  const set = (next: T) => {\n    if (!controlled) setInternal(next);\n    onChange?.(next);\n  };\n  return [current, set] as const;\n}\n\nexport function Chart({\n  title,\n  description,\n  data,\n  series,\n  className,\n  height = 264,\n  includeZero = false,\n  domain,\n  type,\n  area = false,\n  annotations = [],\n  valueFormatter = (value) => formatAnalyticsValue(value),\n  activeIndex,\n  defaultActiveIndex = null,\n  onActiveIndexChange,\n  visibleSeries,\n  defaultVisibleSeries,\n  onVisibleSeriesChange,\n  onDatumActivate,\n  loading = false,\n  empty = \"No data for this range.\",\n  error,\n  showLegend = true,\n  showDataByDefault = false,\n  showGrid = true,\n}: ChartProps) {\n  const id = useId();\n  const [currentIndex, setCurrentIndex] = useControllableValue<number | null>({\n    value: activeIndex,\n    defaultValue: clampAnalyticsIndex(defaultActiveIndex, data.length),\n    onChange: onActiveIndexChange,\n  });\n  const initialSeries = defaultVisibleSeries ?? series.map((item) => item.id);\n  const [currentVisibleSeries, setCurrentVisibleSeries] = useControllableValue<readonly string[]>({\n    value: visibleSeries,\n    defaultValue: initialSeries,\n    onChange: onVisibleSeriesChange,\n  });\n  const [showData, setShowData] = useState(showDataByDefault);\n  const seriesIds = useMemo(() => series.map((item) => item.id), [series]);\n  const seriesIdSet = useMemo(() => new Set(seriesIds), [seriesIds]);\n  const resolvedVisibleSeries = useMemo(() => {\n    if (visibleSeries !== undefined) return currentVisibleSeries;\n    const retained = currentVisibleSeries.filter((seriesId) => seriesIdSet.has(seriesId));\n    return retained.length > 0 ? retained : seriesIds;\n  }, [currentVisibleSeries, seriesIdSet, seriesIds, visibleSeries]);\n  const resolvedVisibleSeriesSet = useMemo(() => new Set(resolvedVisibleSeries), [resolvedVisibleSeries]);\n  const visible = useMemo(() => series.filter((item) => resolvedVisibleSeriesSet.has(item.id)), [resolvedVisibleSeriesSet, series]);\n  const chartType = type ?? (area ? \"area\" : \"line\");\n  const usesBands = chartType === \"bar\" || chartType === \"stacked-bar\";\n  const resolvedDomain = useMemo(\n    () => chartType === \"stacked-bar\"\n      ? getStackedAnalyticsDomain(data, visible.map((item) => item.id), { includeZero: true, domain })\n      : getAnalyticsDomain(data, visible.map((item) => item.id), { includeZero: usesBands || includeZero, domain }),\n    [chartType, data, domain, includeZero, usesBands, visible],\n  );\n  const plotBox = chartBox;\n  const ticks = createAnalyticsTicks(resolvedDomain, 5);\n  const active = clampAnalyticsIndex(currentIndex, data.length);\n  const activeDatum = active === null ? null : data[active];\n  const instructionsId = `${id}-instructions`;\n  const summaryId = `${id}-summary`;\n  const tableId = `${id}-table`;\n  const chartStyle = { \"--teum-chart-height\": `${height}px` } as CSSProperties;\n  const xTicks = uniqueTickIndexes(data.length);\n\n  const moveTo = (next: number | null) => setCurrentIndex(clampAnalyticsIndex(next, data.length));\n  const handlePointerMove = (event: PointerEvent<HTMLDivElement>) => {\n    if (!data.length) return;\n    const rect = event.currentTarget.getBoundingClientRect();\n    const left = rect.width * plotBox.left / plotBox.width;\n    const right = rect.width * plotBox.right / plotBox.width;\n    const plotWidth = Math.max(1, rect.width - left - right);\n    const ratio = Math.min(1, Math.max(0, (event.clientX - rect.left - left) / plotWidth));\n    moveTo(usesBands ? Math.min(data.length - 1, Math.floor(ratio * data.length)) : Math.round(ratio * (data.length - 1)));\n  };\n  const handleKeyDown = (event: KeyboardEvent<HTMLDivElement>) => {\n    if (!data.length) return;\n    const startingIndex = active ?? data.length - 1;\n    let next: number | null = null;\n    if (event.key === \"ArrowLeft\" || event.key === \"ArrowDown\") next = startingIndex - 1;\n    if (event.key === \"ArrowRight\" || event.key === \"ArrowUp\") next = startingIndex + 1;\n    if (event.key === \"Home\") next = 0;\n    if (event.key === \"End\") next = data.length - 1;\n    if (event.key === \"Escape\") next = null;\n    if (event.key === \"Enter\" && activeDatum && active !== null && onDatumActivate) {\n      event.preventDefault();\n      onDatumActivate(activeDatum, active);\n      return;\n    }\n    if (next !== null || event.key === \"Escape\") {\n      event.preventDefault();\n      moveTo(next);\n    }\n  };\n  const toggleSeries = (seriesId: string) => {\n    if (resolvedVisibleSeries.includes(seriesId)) {\n      if (resolvedVisibleSeries.length === 1) return;\n      setCurrentVisibleSeries(resolvedVisibleSeries.filter((id) => id !== seriesId));\n      return;\n    }\n    setCurrentVisibleSeries(series.filter((item) => resolvedVisibleSeries.includes(item.id) || item.id === seriesId).map((item) => item.id));\n  };\n\n  return (\n    <figure className={cn(\"teum-chart\", className)} data-chart-type={chartType} style={chartStyle} aria-labelledby={`${id}-title`} aria-describedby={summaryId}>\n      <figcaption className=\"teum-chart__header\">\n        <div><h3 id={`${id}-title`}>{title}</h3>{description && <p>{description}</p>}</div>\n        <button type=\"button\" className=\"teum-chart__data-toggle\" aria-expanded={showData} aria-controls={tableId} onClick={() => setShowData((current) => !current)}>{showData ? \"Hide data\" : \"View data\"}</button>\n      </figcaption>\n      {showLegend && series.length > 0 && <div className=\"teum-chart__legend\" aria-label={`${title} series`}>\n        {series.map((item) => {\n          const selected = resolvedVisibleSeries.includes(item.id);\n          return <button type=\"button\" key={item.id} data-tone={item.tone ?? \"primary\"} data-line-style={item.lineStyle ?? \"solid\"} aria-pressed={selected} aria-disabled={selected && resolvedVisibleSeries.length === 1 || undefined} onClick={() => toggleSeries(item.id)}><span aria-hidden=\"true\" />{item.label}</button>;\n        })}\n      </div>}\n      <p id={summaryId} className=\"teum-sr-only\">{visible.map((item) => summarizeAnalyticsSeries(data, item, valueFormatter)).join(\" \")}</p>\n      <p id={instructionsId} className=\"teum-sr-only\">Use Left and Right Arrow keys to inspect values. Use Home and End to jump. Press Escape to clear.{onDatumActivate ? \" Press Enter to open the active value.\" : \"\"}</p>\n      <div\n        className=\"teum-chart__plot\"\n        role=\"group\"\n        aria-roledescription=\"interactive chart\"\n        aria-label={`${title}. ${data.length} data points.`}\n        aria-describedby={instructionsId}\n        tabIndex={data.length && !loading && !error ? 0 : -1}\n        onPointerMove={handlePointerMove}\n        onPointerLeave={() => moveTo(null)}\n        onKeyDown={handleKeyDown}\n        onClick={() => { if (activeDatum && active !== null) onDatumActivate?.(activeDatum, active); }}\n      >\n        {loading ? <div className=\"teum-chart__loading\" role=\"status\"><span aria-hidden=\"true\" />Loading chart</div> : error ? <div className=\"teum-chart__error\" role=\"alert\">{error}</div> : !data.length || !visible.length ? <div className=\"teum-chart__empty\">{empty}</div> : <>\n          <svg viewBox={`0 0 ${plotBox.width} ${plotBox.height}`} preserveAspectRatio=\"none\" aria-hidden=\"true\" focusable=\"false\">\n            {showGrid && <g className=\"teum-chart__grid\">\n              {ticks.map((tick) => {\n                const ratio = (tick - resolvedDomain[0]) / Math.max(Number.EPSILON, resolvedDomain[1] - resolvedDomain[0]);\n                const y = plotBox.top + (1 - ratio) * (plotBox.height - plotBox.top - plotBox.bottom);\n                return <g key={tick}><line x1={plotBox.left} x2={plotBox.width - plotBox.right} y1={y} y2={y} /><text x={plotBox.left - 10} y={y}>{valueFormatter(tick, visible[0])}</text></g>;\n              })}\n            </g>}\n            <g className=\"teum-chart__x-axis\">\n              {xTicks.map((index) => {\n                const point = usesBands ? null : getAnalyticsPointPosition(data, index, visible[0].id, resolvedDomain, plotBox);\n                const x = usesBands\n                  ? getAnalyticsBandPosition(data.length, index, plotBox).center\n                  : point?.x ?? plotBox.left + (data.length <= 1 ? 0.5 : index / (data.length - 1)) * (plotBox.width - plotBox.left - plotBox.right);\n                return <text key={data[index]?.id ?? index} x={x} y={plotBox.height - 8} textAnchor={usesBands ? \"middle\" : index === 0 ? \"start\" : index === data.length - 1 ? \"end\" : \"middle\"}>{data[index]?.label}</text>;\n              })}\n            </g>\n            {chartType === \"area\" && visible[0] && <path className=\"teum-chart__area\" data-tone={visible[0].tone ?? \"primary\"} d={createAnalyticsAreaPath(data, visible[0].id, resolvedDomain, plotBox)} />}\n            <g className=\"teum-chart__annotations\">\n              {annotations.map((annotation) => {\n                const point = getAnalyticsPointPosition(data, Math.min(data.length - 1, Math.max(0, annotation.index)), visible[0].id, resolvedDomain, plotBox);\n                if (!point) return null;\n                return <g key={annotation.id} data-tone={annotation.tone ?? \"neutral\"}><line x1={point.x} x2={point.x} y1={plotBox.top} y2={plotBox.height - plotBox.bottom} /><text x={point.x + 5} y={plotBox.top + 9}>{annotation.label}</text></g>;\n              })}\n            </g>\n            {usesBands ? <g className=\"teum-chart__bars\">\n              {data.flatMap((datum, datumIndex) => {\n                const band = getAnalyticsBandPosition(data.length, datumIndex, plotBox);\n                const groupWidth = Math.min(band.width * .72, 48);\n                let positive = 0;\n                let negative = 0;\n                return visible.flatMap((item, seriesIndex) => {\n                  const value = datum.values[item.id];\n                  if (typeof value !== \"number\" || !Number.isFinite(value)) return [];\n                  let startValue = 0;\n                  let endValue = value;\n                  let x = band.center - groupWidth / 2;\n                  let width = groupWidth;\n                  if (chartType === \"stacked-bar\") {\n                    startValue = value >= 0 ? positive : negative;\n                    endValue = startValue + value;\n                    if (value >= 0) positive = endValue;\n                    else negative = endValue;\n                  } else {\n                    width = Math.max(2, groupWidth / Math.max(1, visible.length) - 2);\n                    x = band.center - groupWidth / 2 + seriesIndex * (groupWidth / Math.max(1, visible.length)) + 1;\n                  }\n                  const plotHeight = plotBox.height - plotBox.top - plotBox.bottom;\n                  const scaleY = (next: number) => plotBox.top + (1 - (next - resolvedDomain[0]) / Math.max(Number.EPSILON, resolvedDomain[1] - resolvedDomain[0])) * plotHeight;\n                  const y1 = scaleY(startValue);\n                  const y2 = scaleY(endValue);\n                  return <rect key={`${datum.id}-${item.id}`} data-tone={item.tone ?? \"primary\"} x={x} y={Math.min(y1, y2)} width={width} height={Math.max(1, Math.abs(y2 - y1))} rx={Math.min(4, width / 2)} />;\n                });\n              })}\n            </g> : <g className=\"teum-chart__series\">\n              {visible.map((item) => <path key={item.id} data-tone={item.tone ?? \"primary\"} data-line-style={item.lineStyle ?? \"solid\"} d={createAnalyticsPath(data, item.id, resolvedDomain, plotBox)} pathLength={1} />)}\n            </g>}\n            {activeDatum && active !== null && <g className=\"teum-chart__active\">\n              {(() => {\n                if (usesBands) {\n                  const band = getAnalyticsBandPosition(data.length, active, plotBox);\n                  return <rect className=\"teum-chart__active-band\" x={band.start + 1} y={plotBox.top} width={Math.max(1, band.width - 2)} height={plotBox.height - plotBox.top - plotBox.bottom} rx={5} />;\n                }\n                const first = getAnalyticsPointPosition(data, active, visible[0].id, resolvedDomain, plotBox);\n                return first ? <line x1={first.x} x2={first.x} y1={plotBox.top} y2={plotBox.height - plotBox.bottom} /> : null;\n              })()}\n              {!usesBands && visible.map((item) => {\n                const point = getAnalyticsPointPosition(data, active, item.id, resolvedDomain, plotBox);\n                return point ? <circle key={item.id} data-tone={item.tone ?? \"primary\"} cx={point.x} cy={point.y} r={3.5} /> : null;\n              })}\n            </g>}\n          </svg>\n          {activeDatum && active !== null && <div className=\"teum-chart__tooltip\" aria-hidden=\"true\" style={{ \"--teum-chart-active-x\": `${(usesBands ? getAnalyticsBandPosition(data.length, active, plotBox).center : getAnalyticsPointPosition(data, active, visible[0].id, resolvedDomain, plotBox)?.x ?? plotBox.width / 2) / plotBox.width * 100}%` } as CSSProperties}>\n            <strong>{activeDatum.label}</strong>\n            {visible.map((item) => {\n              const value = activeDatum.values[item.id];\n              return <span key={item.id}><i data-tone={item.tone ?? \"primary\"} aria-hidden=\"true\" /><em>{item.label}</em><b>{typeof value === \"number\" ? valueFormatter(value, item) : \"No data\"}</b></span>;\n            })}\n          </div>}\n        </>}\n      </div>\n      <span className=\"teum-sr-only\" aria-live=\"polite\" aria-atomic=\"true\">{activeDatum ? describeAnalyticsDatum(activeDatum, visible, valueFormatter) : \"\"}</span>\n      <div id={tableId} className={cn(\"teum-chart__table\", !showData && \"teum-chart__table--visually-hidden\")}>\n        <table>\n          <caption>{title} data</caption>\n          <thead><tr><th scope=\"col\">Period</th>{visible.map((item) => <th scope=\"col\" key={item.id}>{item.label}</th>)}</tr></thead>\n          <tbody>{data.map((datum) => <tr key={datum.id}><th scope=\"row\">{datum.label}</th>{visible.map((item) => <td key={item.id}>{typeof datum.values[item.id] === \"number\" ? valueFormatter(datum.values[item.id] as number, item) : \"No data\"}</td>)}</tr>)}</tbody>\n        </table>\n      </div>\n    </figure>\n  );\n}\n",
      "type": "registry:component",
      "target": "components/ui/chart.tsx"
    },
    {
      "path": "registry/styles/components/chart.css",
      "content": "/* Generated from src/foundation.css and src/styles.css. Do not edit directly. */\n\n@layer teum.tokens, teum.base, teum.components;\n\n@layer teum.components {\n.teum-chart { display: grid; width: 100%; min-width: 0; margin: 0; gap: 12px; }\n\n.teum-chart__header { display: flex; min-width: 0; align-items: flex-start; justify-content: space-between; gap: 20px; }\n\n.teum-chart__header > div { display: grid; min-width: 0; gap: 3px; }\n\n.teum-chart__header h3 { margin: 0; color: var(--teum-fg-default); font-size: var(--teum-type-section); font-weight: 610; letter-spacing: -.022em; }\n\n.teum-chart__header p { margin: 0; color: var(--teum-fg-muted); font-size: var(--teum-type-metadata); line-height: 1.45; }\n\n.teum-chart__data-toggle {\n  min-height: 28px;\n  flex: none;\n  padding: 0 8px;\n  border: 0;\n  border-radius: var(--teum-radius-control);\n  background: transparent;\n  color: var(--teum-fg-muted);\n  font: inherit;\n  font-size: var(--teum-type-metadata);\n  cursor: pointer;\n  transition: background-color var(--teum-hover-duration) var(--teum-ease), color var(--teum-hover-duration) var(--teum-ease);\n}\n\n.teum-chart__legend { display: flex; min-width: 0; flex-wrap: wrap; gap: 4px; }\n\n.teum-chart__legend button {\n  display: inline-flex;\n  min-height: 26px;\n  align-items: center;\n  gap: 6px;\n  padding: 0 7px;\n  border: 0;\n  border-radius: var(--teum-radius-control);\n  background: transparent;\n  color: var(--teum-fg-muted);\n  font: inherit;\n  font-size: var(--teum-type-metadata);\n  cursor: pointer;\n  transition: background-color var(--teum-hover-duration) var(--teum-ease), color var(--teum-hover-duration) var(--teum-ease), opacity var(--teum-hover-duration) var(--teum-ease);\n}\n\n.teum-chart__legend button > span { width: 16px; height: 2px; border-radius: 999px; background: var(--teum-fg-default); }\n\n.teum-chart__legend button[data-tone=\"secondary\"] > span { opacity: .56; }\n\n.teum-chart__legend button[data-tone=\"tertiary\"] > span { opacity: .32; }\n\n.teum-chart__legend button[data-line-style=\"dashed\"] > span { background: repeating-linear-gradient(90deg, currentColor 0 5px, transparent 5px 8px); }\n\n.teum-chart__legend button[data-line-style=\"dotted\"] > span { background: repeating-linear-gradient(90deg, currentColor 0 2px, transparent 2px 5px); }\n\n.teum-chart__legend button[aria-pressed=\"false\"] > span { opacity: .28; }\n\n.teum-chart__legend button[aria-disabled=\"true\"] { cursor: default; }\n\n.teum-chart__plot {\n  position: relative;\n  min-width: 0;\n  height: var(--teum-chart-height);\n  overflow: hidden;\n  border-radius: var(--teum-radius-container);\n  background: color-mix(in srgb, var(--teum-bg-stage) 76%, transparent);\n  box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--teum-fg-default) 7%, transparent);\n}\n\n.teum-chart__plot:focus { outline: 0; }\n\n.teum-chart__plot:focus-visible { box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--teum-fg-default) 32%, transparent); }\n\n.teum-chart__plot svg { display: block; width: 100%; height: 100%; overflow: visible; }\n\n.teum-chart__grid line { stroke: var(--teum-separator); stroke-width: 1; vector-effect: non-scaling-stroke; }\n\n.teum-chart__grid text,\n.teum-chart__x-axis text { fill: var(--teum-fg-subtle); font-family: var(--teum-font-ui); font-size: 10px; font-variant-numeric: tabular-nums; }\n\n.teum-chart__grid text { text-anchor: end; dominant-baseline: middle; }\n\n.teum-chart__area { fill: var(--teum-fg-default); opacity: .06; }\n\n.teum-chart__series path { fill: none; stroke: var(--teum-fg-default); stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.65; vector-effect: non-scaling-stroke; transition: opacity var(--teum-fast) var(--teum-ease); }\n\n.teum-chart__series path[data-tone=\"secondary\"] { opacity: .58; }\n\n.teum-chart__series path[data-tone=\"tertiary\"] { opacity: .34; }\n\n.teum-chart__series path[data-line-style=\"dashed\"] { stroke-dasharray: 6 5; }\n\n.teum-chart__series path[data-line-style=\"dotted\"] { stroke-dasharray: 1.5 4; }\n\n.teum-chart__bars rect {\n  fill: var(--teum-fg-default);\n  shape-rendering: geometricPrecision;\n  transition: opacity var(--teum-hover-duration) var(--teum-ease);\n}\n\n.teum-chart__bars rect[data-tone=\"secondary\"] { opacity: .58; }\n\n.teum-chart__bars rect[data-tone=\"tertiary\"] { opacity: .32; }\n\n.teum-chart__annotations line { stroke: var(--teum-fg-subtle); stroke-dasharray: 2 4; stroke-width: 1; vector-effect: non-scaling-stroke; }\n\n.teum-chart__annotations text { fill: var(--teum-fg-subtle); font-family: var(--teum-font-ui); font-size: 9px; }\n\n.teum-chart__annotations [data-tone=\"danger\"] line { stroke: var(--teum-fg-danger); }\n\n.teum-chart__annotations [data-tone=\"danger\"] text { fill: var(--teum-fg-danger); }\n\n.teum-chart__active line { stroke: color-mix(in srgb, var(--teum-fg-default) 22%, transparent); stroke-width: 1; vector-effect: non-scaling-stroke; }\n\n.teum-chart__active .teum-chart__active-band { fill: color-mix(in srgb, var(--teum-fg-default) 4%, transparent); stroke: none; }\n\n.teum-chart__active circle { fill: var(--teum-bg-surface); stroke: var(--teum-fg-default); stroke-width: 1.5; vector-effect: non-scaling-stroke; }\n\n.teum-chart__active circle[data-tone=\"secondary\"] { opacity: .65; }\n\n.teum-chart__active circle[data-tone=\"tertiary\"] { opacity: .45; }\n\n.teum-chart__tooltip {\n  position: absolute;\n  top: 12px;\n  left: clamp(82px, var(--teum-chart-active-x), calc(100% - 82px));\n  z-index: 2;\n  display: grid;\n  min-width: 144px;\n  gap: 6px;\n  padding: 9px 10px;\n  border-radius: 8px;\n  background: var(--teum-bg-flyout);\n  box-shadow: var(--teum-shadow-flyout);\n  color: var(--teum-fg-default);\n  pointer-events: none;\n  transform: translateX(-50%);\n}\n\n.teum-chart__tooltip > strong { font-size: var(--teum-type-metadata); font-weight: 590; }\n\n.teum-chart__tooltip > span { display: grid; grid-template-columns: 7px minmax(0, 1fr) auto; align-items: center; gap: 6px; font-size: var(--teum-type-metadata); }\n\n.teum-chart__tooltip i { width: 6px; height: 6px; border-radius: 50%; background: var(--teum-fg-default); }\n\n.teum-chart__tooltip i[data-tone=\"secondary\"] { opacity: .58; }\n\n.teum-chart__tooltip i[data-tone=\"tertiary\"] { opacity: .34; }\n\n.teum-chart__tooltip em { overflow: hidden; color: var(--teum-fg-muted); font-style: normal; text-overflow: ellipsis; white-space: nowrap; }\n\n.teum-chart__tooltip b { font-variant-numeric: tabular-nums; font-weight: 580; }\n\n.teum-chart__loading,\n.teum-chart__empty,\n.teum-chart__error { display: grid; width: 100%; height: 100%; place-content: center; color: var(--teum-fg-subtle); font-size: var(--teum-type-ui); }\n\n.teum-chart__error { color: var(--teum-fg-danger); }\n\n.teum-chart__loading { grid-auto-flow: column; align-items: center; gap: 8px; }\n\n.teum-chart__loading > span { width: 12px; height: 12px; border: 1.5px solid var(--teum-separator-strong); border-top-color: var(--teum-fg-default); border-radius: 50%; animation: teum-spinner .7s linear infinite; }\n\n.teum-chart__table { min-width: 0; overflow: auto; border-radius: var(--teum-radius-container); background: var(--teum-bg-stage); }\n\n.teum-chart__table--visually-hidden { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); clip-path: inset(50%); white-space: nowrap; }\n\n.teum-chart__table table { width: 100%; border-collapse: collapse; font-size: var(--teum-type-metadata); font-variant-numeric: tabular-nums; }\n\n.teum-chart__table caption { padding: 10px 12px; text-align: left; color: var(--teum-fg-default); font-weight: 590; }\n\n.teum-chart__table th,\n.teum-chart__table td { padding: 8px 12px; border-top: 1px solid var(--teum-separator); text-align: right; white-space: nowrap; }\n\n.teum-chart__table th:first-child { text-align: left; }\n\n.teum-chart__table thead th { color: var(--teum-fg-subtle); font-weight: 540; }\n\n.teum-chart__table tbody th { color: var(--teum-fg-muted); font-weight: 520; }\n\n.teum-chart__data-toggle:focus,\n.teum-chart__legend button:focus { outline: 0; }\n\n.teum-chart__data-toggle:focus-visible,\n.teum-chart__legend button:focus-visible {\n  box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--teum-focus-control) 42%, transparent);\n}\n\n@media (hover: hover) and (pointer: fine) {\n.teum-chart__data-toggle:hover,\n.teum-chart__legend button:not([aria-disabled=\"true\"]):hover { background: var(--teum-hover); color: var(--teum-fg-default); }\n}\n\n.teum-chart__data-toggle:active,\n.teum-chart__legend button:not([aria-disabled=\"true\"]):active { transform: scale(.985); }\n\n@media (max-width: 640px) {\n.teum-chart__header { align-items: center; }\n\n.teum-chart__plot { height: min(var(--teum-chart-height), 230px); }\n\n.teum-chart__tooltip { min-width: 128px; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n.teum-chart__loading > span { animation: none; }\n\n.teum-chart__series path,\n.teum-chart__bars rect { transition-duration: 0ms; }\n}\n\n@keyframes teum-spin { to { transform: rotate(360deg); } }\n}\n",
      "type": "registry:style",
      "target": "styles/components/chart.css"
    }
  ],
  "meta": {
    "teum": {
      "contract": "/agent/teum-agent.json",
      "registryItem": "@teum/chart"
    }
  },
  "type": "registry:component"
}
