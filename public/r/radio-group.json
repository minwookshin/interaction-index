{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "radio-group",
  "title": "Radio Group",
  "description": "Accessible mutually exclusive choices with descriptions and validation.",
  "dependencies": [
    "@base-ui/react"
  ],
  "registryDependencies": [
    "@index/interaction-index-base"
  ],
  "files": [
    {
      "path": "registry/components/ui/radio-group.tsx",
      "content": "import \"../../styles/index-base.css\";\nimport \"../../styles/components/radio-group.css\";\nimport { Radio } from \"@base-ui/react/radio\";\nimport { RadioGroup as RadioGroupPrimitive } from \"@base-ui/react/radio-group\";\nimport { useId } from \"react\";\nimport { cn } from \"../../lib/cn\";\n\nexport type RadioOption = {\n  value: string;\n  label: string;\n  description?: string;\n  disabled?: boolean;\n};\n\nexport type RadioGroupProps = Omit<RadioGroupPrimitive.Props<string>, \"children\"> & {\n  label: string;\n  description?: string;\n  error?: string;\n  options: readonly RadioOption[];\n  orientation?: \"vertical\" | \"horizontal\";\n};\n\nexport function RadioGroup({ className, label, description, error, options, orientation = \"vertical\", ...props }: RadioGroupProps) {\n  const id = useId();\n  const descriptionId = description ? `${id}-description` : undefined;\n  const errorId = error ? `${id}-error` : undefined;\n\n  return (\n    <div className=\"ix-radio-field\" data-invalid={Boolean(error) || undefined}>\n      <div id={id} className=\"ix-radio-field__label\">{label}</div>\n      {description && <div id={descriptionId} className=\"ix-field__description\">{description}</div>}\n      <RadioGroupPrimitive\n        aria-labelledby={id}\n        aria-describedby={[descriptionId, errorId].filter(Boolean).join(\" \") || undefined}\n        aria-invalid={Boolean(error) || undefined}\n        className={cn(\"ix-radio-group\", `ix-radio-group--${orientation}`, className)}\n        {...props}\n      >\n        {options.map((option) => (\n          <label className=\"ix-radio-option\" key={option.value} data-disabled={option.disabled || undefined}>\n            <Radio.Root value={option.value} disabled={option.disabled} className=\"ix-radio\">\n              <Radio.Indicator className=\"ix-radio__indicator\" />\n            </Radio.Root>\n            <span><strong>{option.label}</strong>{option.description && <small>{option.description}</small>}</span>\n          </label>\n        ))}\n      </RadioGroupPrimitive>\n      {error && <div id={errorId} className=\"ix-field__error\">{error}</div>}\n    </div>\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/ui/radio-group.tsx"
    },
    {
      "path": "registry/styles/components/radio-group.css",
      "content": "/* Generated from src/styles.css. Do not edit directly. */\n\n@layer index.tokens, index.base, index.components;\n\n@layer index.components {\n.ix-radio-field { display: grid; gap: 6px; min-width: 220px; }\n\n.ix-radio-field__label { color: var(--ix-ink); font-size: var(--ix-type-label); font-weight: 540; }\n\n.ix-radio-group { display: flex; gap: 4px; }\n\n.ix-radio-option { display: flex; min-height: 32px; align-items: flex-start; gap: 9px; padding: 6px 0; color: var(--ix-ink); cursor: pointer; }\n\n.ix-radio-option > span { display: grid; gap: 2px; line-height: 1.3; }\n\n.ix-radio-option strong { font-size: var(--ix-type-ui); font-weight: 480; }\n\n.ix-radio-option small { max-width: 280px; color: var(--ix-ink-tertiary); font-size: var(--ix-type-metadata); }\n\n.ix-radio-option[data-disabled] { color: var(--ix-ink-disabled); cursor: not-allowed; }\n\n.ix-radio {\n  display: grid;\n  width: 16px;\n  height: 16px;\n  margin-top: 1px;\n  flex: 0 0 auto;\n  place-items: center;\n  border: 1px solid var(--ix-border-strong);\n  border-radius: 50%;\n  background: var(--ix-control-surface);\n  cursor: inherit;\n  transition: border-color var(--ix-hover-duration) ease, background-color var(--ix-hover-duration) ease, transform var(--ix-press-duration) var(--ix-ease-out, ease-out);\n}\n\n.ix-radio[data-checked] { border-color: var(--ix-inverse); background: var(--ix-inverse); }\n\n.ix-radio__indicator { width: 6px; height: 6px; border-radius: 50%; background: var(--ix-on-inverse); }\n\n.ix-radio:focus-visible { outline: 2px solid var(--ix-focus-control); outline-offset: 2px; }\n\n.ix-radio[data-disabled] { border-color: var(--ix-border); background: var(--ix-gray-100); }\n\n@media (hover: hover) and (pointer: fine) {\n.ix-radio-option:hover .ix-radio:not([data-disabled]) { border-color: var(--ix-gray-700); }\n}\n\n.ix-radio:active { transform: scale(0.97); }\n\n@media (prefers-reduced-motion: reduce) {\n.ix-radio { transition-duration: 0.01ms; }\n}\n\n.ix-field__description,\n.ix-field__error { color: var(--ix-ink-tertiary); font-size: 11.5px; line-height: 1.45; }\n\n.ix-field__error { color: var(--ix-fg-default); font-weight: 520; }\n\n:root[data-theme=\"dark\"] .ix-radio[data-disabled] { background: rgb(255 255 255 / 0.025); color: var(--ix-ink-disabled); }\n\n:root[data-theme=\"dark\"] .ix-radio[data-checked] { box-shadow: none; }\n\n:root[data-theme=\"dark\"] .ix-field__error { color: var(--ix-ink); }\n\n.ix-radio__indicator {\n  opacity: 1;\n  transform: scale(1);\n  transition: opacity 140ms var(--ix-ease-out), transform 140ms var(--ix-ease-out);\n}\n\n@starting-style {\n.ix-radio__indicator {\n    opacity: 0;\n    transform: scale(.72);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n.ix-radio__indicator { transition-duration: 0.01ms; }\n}\n\n@media (prefers-reduced-motion: reduce) {\n.ix-radio:active { transform: none; }\n\n.ix-radio__indicator { transition-duration: 0.01ms; }\n}\n}\n",
      "type": "registry:style",
      "target": "styles/components/radio-group.css"
    }
  ],
  "type": "registry:ui"
}