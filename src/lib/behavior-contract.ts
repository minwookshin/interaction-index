export type BehaviorContract = {
  input: readonly string[];
  origin: string;
  enter: string;
  active: string;
  exit: string;
  interruption: string;
  keyboard: readonly string[];
  reducedMotion: string;
};
